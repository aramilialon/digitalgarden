# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
- task: Bash@3
  inputs:
    targetType: 'inline'
    script: 'pip install mkdocs-material'

- task: Bash@3
  inputs:
    targetType: 'inline'
    script: 'mkdocs build'

- task: SSH@0
  inputs:
    sshEndpoint: 'digitalgarden-maggiolo-net'
    runOptions: 'commands'
    commands: 'mkdir -p ~/digitalgarden'
    readyTimeout: '20000'

- task: CopyFilesOverSSH@0
  inputs:
    sshEndpoint: 'digitalgarden-maggiolo-net'
    sourceFolder: './site'
    contents: '**'
    targetFolder: '~/digitalgarden'
    cleanTargetFolder: true
    cleanHiddenFilesInTarget: true
    readyTimeout: '20000'

- task: SSH@0
  inputs:
    sshEndpoint: 'digitalgarden-maggiolo-net'
    runOptions: 'commands'
    commands: 'sudo cp -r * /var/www/html/digitalgarden/site/'
    readyTimeout: '20000'